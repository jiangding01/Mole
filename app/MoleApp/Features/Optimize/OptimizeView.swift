import MoleKit
import SwiftUI

/// 优化页（设计 §5.3，UI 规格页面 4）：一键流水线为默认路径。
struct OptimizeView: View {
    var body: some View {
        PagePlaceholder(title: "优化", designRef: "§5.3")
        // TODO(Phase 3): 一键流水线 + 分组任务流 + 自定义入口 + 累计次数
    }
}
