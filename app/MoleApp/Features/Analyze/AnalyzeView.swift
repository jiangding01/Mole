import MoleKit
import SwiftUI

/// 分析页（设计 §5.4，UI 规格页面 5）：Treemap + 左栏列表 + 面包屑。
struct AnalyzeView: View {
    var body: some View {
        PagePlaceholder(title: "分析", designRef: "§5.4")
        // TODO(Phase 3): analyze --serve 客户端、Squarified Treemap、小项聚合块
    }
}
